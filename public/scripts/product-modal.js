// Этот файл теперь можно не подключать на странице каталога,
// так как логика модалки перенесена в catalog.js.
// Если он используется на других страницах — оставь как есть.

// Если всё же хочешь использовать его как универсальный хелпер:
import { lockScroll, unlockScroll } from '/scripts/scroll-lock.js';

export function initProductModals() {
  // Для каталога эта функция теперь пустая — логика в initCatalogPage
  // Оставляем экспорт для совместимости
}